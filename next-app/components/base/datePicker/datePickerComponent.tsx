import React, { FunctionComponent, ReactElement } from 'react';

/**
 * Basic date picker.
 *
 * @returns {ReactElement} The date picker component.
 */
const DatePicker: FunctionComponent = (): ReactElement => {
    return <></>;
};

export default DatePicker;
