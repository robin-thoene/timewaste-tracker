import React, { FunctionComponent, ReactElement } from 'react';

/**
 * Displays data as graphical tag.
 *
 * @returns {ReactElement} The tag component.
 */
const Tag: FunctionComponent = (): ReactElement => {
    return <></>;
};

export default Tag;
